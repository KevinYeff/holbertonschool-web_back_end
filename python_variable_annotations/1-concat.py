#!/usr/bin/env python3
"""Task 1 Basic annotation - concat"""


def concat(str1: str, str2: str) -> str:
    """A type annotated function that takes 2
    strings as arguments and specifies the returns
    as a concatenated string"""
    return str1 + str2
